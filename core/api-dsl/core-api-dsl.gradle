apply {
    from script('libraries')
    from script('groovy')
}

dependencies {
    compile project(':core:util')
}

sonar {
    project {
        language = 'groovy'
    }
}

setupFileEncoding(tasks)
