repositories { mavenCentral() }

ext {
    libraries = [
        spock: dependencies.create('org.spockframework:spock-core:+'),
        groovy: dependencies.create('org.codehaus.groovy:groovy-all:+')
    ]
}
