rootProject.name = 'api-fake-tool'

['core',
 'api-dsl',
 'fake-server',
 'fake-client'].each { sub ->
    include sub
    project(":${sub}").with {
        buildFileName = "${sub.replace(':', '-')}.gradle"
    }
}
